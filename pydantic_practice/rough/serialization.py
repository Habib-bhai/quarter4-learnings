from pydantic import BaseModel
from typing import List
from datetime import datetime



# we use modelname.mode_dump() method or modelname.model_dump_json() methods to convert pydantic models into dictionaries.
